# このフォルダは、"山の端ビューア"のプロジェクトをデプロイしている場所です。

##  フォルダの構成
./SJ : 東京北部に視点を設定したビューアのプログラムやデータが置いてあります
./NC : 練馬区役所展望ロビーに視点を設定したビューアのプログラムやデータが置いてあります
./TC : 東京都庁展望室に視点を設定したビューアのプログラムやデータが置いてあります
なお、これらは独立したプログラムで、完成されたものであり、ファイルの改変は行わないでください。

./  : 上記3つのビューアを切り替えて表示する「枠」のページが置いてあります。
      - index.html : 各ビューアを iframe で読み込み、ビューア側のヘッダーを隠して、
                     代わりに視点リストボックス付きのヘッダーを表示します。
                     視点は URL の ?v=SJ|NC|TC → 前回の選択（localStorage）→ 既定値（東京北部）の順に決まります。
      - sw.js / manifest.json : ルート用。sw.js はルート直下のファイルだけを扱い、サブフォルダには関与しません。
